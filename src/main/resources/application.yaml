spring:
  application:
    name: proyecto-final-backend
  flyway:
    baseline-on-migrate: true # Esto porque la db ya existe y tiene datos
  profiles:
    active: dev
  jwt:
    secret: ${SPRING_JWT_SECRET}
    accessTokenExpiration: 900 # 15 minutes
    refreshTokenExpiration: 604800 # 7 days
